# Domains the runtime firewall (init-firewall.sh) resolves and allows outbound.
# GitHub IP ranges are fetched dynamically by init-firewall.sh — no need to list
# github.com here. One host per line; blank lines and #-comments are ignored.

# === Anthropic API (Claude Code) ===
api.anthropic.com
statsig.anthropic.com
sentry.io
downloads.claude.ai

# === npm ===
registry.npmjs.org
cdn.npmjs.org

# === Docker Hub ===
auth.docker.io
registry-1.docker.io
index.docker.io
production.cloudflare.docker.com
docker.io

# === Project-specific (add your own below) ===
