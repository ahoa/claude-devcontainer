# domains.conf — outbound hosts the runtime firewall allows. EDIT THIS FILE.
#
# One host per line; blank lines and #-comments are ignored. GitHub IP ranges are
# fetched dynamically by .template/init-firewall.sh, so github.com needs no entry.
#
# This file is yours: the installer creates it once and never overwrites it, so
# template updates leave your entries alone. It is baked into the image, so
# start.sh forces a rebuild when you change it.

# === Anthropic API (Claude Code) ===
api.anthropic.com
statsig.anthropic.com
sentry.io
downloads.claude.ai

# === npm ===
registry.npmjs.org
cdn.npmjs.org

# === Docker Hub ===
auth.docker.io
registry-1.docker.io
index.docker.io
production.cloudflare.docker.com
docker.io

# === Project-specific (add your own below) ===
